// Stubs for virtual modules that the TanStack Start Vite plugin normally
// provides. This app is a plain Vite SPA, so these code paths never run.
export const tsrStartManifest = () => ({ routes: {} });

export default { tsrStartManifest };
